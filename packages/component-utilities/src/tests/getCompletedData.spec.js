import { describe, vi, it, expect } from 'vitest';
import getCompletedData from "../getCompletedData.js";

describe('getCompletedData', () => {
    it("should be defined", () => {
        expect(getCompletedData).toBeDefined()
    })
});
