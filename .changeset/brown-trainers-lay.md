---
'@evidence-dev/component-utilities': minor
---

Add built-in duration formats for human-friendly time display. Two new format tags: `duration` (input in minutes) and `duration_hrs` (input in hours) that render values like "14 mins", "2.3 hrs", "1.5 days".
